import { palette } from 'src/theme/secondaryTheme/palette'
import {
  fontWeights,
  fontSizes,
  lineHeights,
} from 'src/theme/primaryTheme/typography'
import { space, radii } from 'src/theme/primaryTheme'

const secondaryTheme = {
  colors: palette,
  fontWeights,
  fontSizes,
  lineHeights,
  space,
  radii,
}

export default secondaryTheme
